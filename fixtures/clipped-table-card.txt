## Conclusion first

The premise doesn't hold as asked. **KORD, KAUS and KDCA are already fully ingested by three of the four pipelines** — they exist as a dedicated *hourly-market* lane (separate from the five daily-scan `CITIES` in `cities.py`, which do NOT include them). Adding them "new" only makes sense for the **daily-scan lane** (NWS/aviationweather per-city polling, wethr SSE push slot, WU PWS hyperlocal ring) — and there the binding limit is unambiguous: **the wethr Professional SSE tier (1 connection / 5 stations, already full)**. Everything else — NWS/METAR rate limits, WU key quota, Kalshi rate limit, disk — has comfortable headroom for 3 more stations. Disk is the second-tightest constraint on a long horizon, not request budget.

## What's already running (the "check before costing" instruction, answered)

`weather-hourly-obs.service`/`.timer` (`/home/ubuntu/.config/systemd/user/weather-hourly-obs.timer`, `weatherbot/hourly_obs_capture.py:1-38`) polls wethr's REST `station_day` endpoint for KORD/KAUS/KDCA every 15 min (whole-day payload, so cadence sets staleness not coverage: "~550 MB/day download, nothing extra stored" per the docstring). `weather-hourly-capture.service`/`.timer` (5-min cadence, `weatherbot/hourly_capture.py`) pulls aviationweather.gov METARs for the same 5 hourly stations (KLAX, KORD, KAUS, KDCA, KNYC — `weatherbot/hourly_markets.py:41-56` `SERIES_STATION`). Both feed `market_history.db`'s hourly tables (`hourly_obs_raw` 185,961 rows, `hourly_metar_raw` 18,458, `kalshi_hourly_odds` 581,061, `kalshi_hourly_depth` 275,657, `kalshi_hourly_trades` 124,385, `hourly_settlements` 5,342). Kalshi's hourly series (`KXTEMPCHIH`, `KXTEMPAUSH`, `KXTEMPDCH`) are already scanned as part of the 60s odds loop's series set. This is 2026-08-12/13-vintage shipped work, not a proposal.

What is genuinely absent for these three: they are not in `weatherbot/cities.py::CITIES` (no daily-high scan, no wethr SSE subscription, no WU PWS ring membership) — that's the only "+3 stations" that costs anything new.

## Per-pipeline table

```
| Pipeline | Current load | Documented/observed limit | Marginal cost of +3 (daily-scan) stations | Headroom |
|---|---|---|---|---|
| **1. NWS/aviationweather METAR** (`weatherbot/clients.py`) | 5 daily cities × several calls/scan-cycle (station meta cached; `latest observation`, METAR 24h, NWS 5-min obs, CLI) on a 5-min bot scan loop. Hourly-lane already adds `aviationweather.gov/api/data/metar` for KORD/KAUS/KDCA every 5 min (`hourly_capture.py:42`, keyless). | No documented published limit found in code/comments; api.weather.gov and aviationweather.gov are both public, unauthenticated, and this repo has never hit a 429 on them (no backoff/tripwire code exists for these clients, unlike wethr's). **Open question: no numeric rate limit found — could not determine one exists.** | ~3 stations × ~4 endpoint calls/5-min cycle ≈ negligible; already-running hourly METAR poll for these exact 3 stations proves the load is tolerated today. | Effectively unconstrained on current evidence; not the binding limit. |
| **2. wethr** (`weatherbot/wethr_web.py`, `wethr_web_watcher.py`, `wethr_stream_daemon.py`) | **SSE push stream: 5/5 stations occupied** — confirmed from `cities.py:CityMarket.stream_subscribed` docstring + `CITIES` tuple: KSFO, KLAX, KMDW, KMIA, KLAS are subscribed; KNYC explicitly demoted `stream_subscribed=False` 2026-08-13, its slot given to KLAS. KORD/KAUS/KDCA hold **no** SSE slot. REST station-day (non-SSE) is what actually serves them today, at 15-min poll / whole-day payload (~183 MB/station/day ≈ 550 MB/day for 3 stations, per `hourly_obs_capture.py` docstring). | Professional tier: **1 connection / 5 stations, hard cap, currently full** (CLAUDE.md, corroborated by `cities.py` docstring and HANDOFF's "EXPANSION PARKED... wethr Professional caps at 1 connection / 5 stations, we are AT it"). REST station-day endpoint is **uncapped** per the same docstring, but its openness to non…