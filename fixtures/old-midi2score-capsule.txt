📁 /home/ubuntu/projects/midi2score  (scouted 22m ago)
aka: m2s (m2s.suchag.com)
what: Converts a cellist/composer's DAW MIDI bounce into a MuseScore-ready score via quantization + orchestral merge; has a live web front end for client Cecilia (project Fang).
stack: Python 3.10+, uv/venv, music21/partitura/mido/numpy; stdlib-only HTTP server (midi2score/web.py) for the front end, no build step.
verify: none — the worker must propose one (no pytest/CI found; evaluation is python -m midi2score.eval or tools/compare_cue.py…
surfaces:
  · midi2score/ — core library: pipeline, quantization (notevalues.py), orchestral merge, staves, mscx…
  · midi2score/web/ — front-end assets (app.css/js, ios.css skin)
  · tools/ — comparison/grading scripts (compare_cue.py, grade_stems.py, key_accuracy.py, make_house_te…
  · docs/ — survey.md, design.md, house-style.md, tie-policy.md: the design record, read before changin…
  · deploy/ — systemd unit, Caddy/nginx config, account setup for the live m2s.suchag.com service
  · data/, handoff/, tests/fixtures/ — gitignored client MIDI/score material; handoff/ is the open-item…
deploy: Push to main on GitHub (private repo, casualsav/midi2score); live service is systemd --user unit m2s-web on this box (127.0.0.1:8324) behind Caddy TLS for m2s.suchag.com — deploy/…
conventions:
  · Read handoff/facts.md (standing truths) and HANDOFF.md (open items) before any nontrivial change — heavy prior-decision history that must n…
  · Real client MIDI/score files under data/ are gitignored and must never be committed; account credentials live outside the repo (~/.local/sh…
  · docs/design.md and docs/survey.md gate pipeline design decisions — proposals should reference them, not bypass them.
hazards:
  · This is a live production web service on this box with real user (Cecilia) data and a signed-cookie auth system — code changes to midi2scor…
  · Git history was rewritten (filter-repo) on 2026-08-09 to scrub credentials; old pre-2026-08-09 commit hashes after the auth.py-introducing…
  · No automated test suite / CI gate exists; correctness is argued via docs/handoff measurements against real client scores, not via a green b…
docs:
  · HANDOFF.md
  · handoff/facts.md
  · docs/design.md
components:
  · midi2score/pipeline.py + notevalues.py — ingest/grid/quantization core, the project's stated hard problem
  · midi2score/orchestral.py + staves.py + voices.py — track-to-stave merge/lexicon for multi-instrument cues
  · midi2score/web.py + web/ — the live client-facing front end (3 screens, stdlib HTTP server, session auth)
  · midi2score/mscx.py + eval.py + metrics.py — MuseScore-file reading and evaluation-harness scoring (MV2H, key accuracy)
  · tools/*.py — standalone grading/comparison CLIs used to validate changes against client ground-truth scores
flows:
  · DAW MIDI bounce (upload via web.py) → pipeline.run/orchestral.build (quantize + merge + desk split) → draft.json / MusicXML score, engraved on confirmation
  · MIDI + finished .mscz ground truth → tools/compare_cue.py (via mscx.py) → per-stave attack/tie comparison report used to grade pipeline changes
  · ASAP dataset MIDI → midi2score.eval → MV2H/key-accuracy metrics vs. MuseScore baseline, the offline regression harness
sources of truth:
  · Live app state (projects, house style, engraved cues) — ~/.local/share/midi2score/ (MIDI2SCORE_DATA overrides), never t…
  · Client accounts/credentials — ~/.local/share/midi2score/accounts.json, shape in deploy/accounts.example.json
  · Canonical client ground-truth scores — paths listed in handoff/facts.md under 'The canonical ground truth per cue' (dat…
  · Nginx/Caddy config on this box is not version-controlled — deploy/ copies are records, not source of truth
vocabulary:
  · cue — one piece/section of the client's score (e.g. 1M1, 1M3), the unit of work, not a generic term
  · desk — a stand of players sharing one stave (e.g. 'Violin I (3)' = 3 players, 1 stave), distinct fr…
  · stem — a per-instrument consolidated MIDI/MuseScore export from the client's DAW, the merge answer…
  · unit — an internal numbered work session referenced throughout handoff/facts.md (e.g. 'unit 21 ship…
do not assume:
  · Do not assume there is a runnable test suite — verify with the worker; evaluation here is measurement/comparison scripts, not pass/fail tes…
  · Do not assume the live web service auto-reloads code changes — deploy/README.md implies restart is needed for code (unlike data/account edi…
  · Do not assume any given handoff/ item is still open — HANDOFF.md is a live index that shrinks as items close; read it fresh rather than fro…
  · Do not assume DNS/Caddy/nginx state described in deploy/README.md is current — it documents a fix applied 2026-08-10 and describes box-wide…
unknown:
  · Whether any lint/type-check config (ruff/mypy) is actually enforced anywhere — none found in pyproj…
  · Exact current open-item count/priority in handoff/ — HANDOFF.md changes frequently and was not full…
⚠ 22 schema violation(s) corrected by the daemon (fields clipped or dropped)